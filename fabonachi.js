x=0,y=1,sum=0;
while(sum < 50)
{
    // console.log(sum);
    // x=y;
    // y=sum +x;
    // sum = x+y;
    // sum = x + y;
    // sum = sum + y;
    console.log(sum);
    sum = x+y;
    
    x = y;
    y = sum;
    
}